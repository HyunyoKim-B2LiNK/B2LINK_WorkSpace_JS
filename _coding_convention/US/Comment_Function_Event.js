
__ElementID(or __ElementReferenceName 또는 목적이나 행동에 대한 키워드 구문등의 해당 이벤트 리스너의 목적을 알 수 있게 작성.

EventListener가 public일 경우는 거의 존재 하지 않음.
 - 존재 할 일이 없는 로직으로 짜여진게 올바름.

//----------------------------------------------------------------------------------------------------;

//	Event Listener - Keyboard;

//----------------------------------------------------------------------------------------------------;

/**
 * @private
 * @function
 * @param {Keyboard} e event
 */
var evt_kDown__ElementID( e ){};

/**
 * @private
 * @function
 * @param {Keyboard} e event
 */
var evt_kUp__ElementID( e ){};

/**
 * @private
 * @function
 * @param {Keyboard} e event
 */
var evt_kDown__ElementReferenceName( e ){};

/**
 * @private
 * @function
 * @param {Keyboard} e event
 */
var evt_kUp__ElementReferenceName( e ){};

//----------------------------------------------------------------------------------------------------;

//	Event Listener - Mouse;

//----------------------------------------------------------------------------------------------------;

/**
 * @private
 * @function
 * @param {MouseEvent} e event
 */
var evt_mClick__ElementID( e ){};

/**
 * @private
 * @function
 * @param {MouseEvent} e event
 */
var evt_mDown__ElementID( e ){};

/**
 * @private
 * @function
 * @param {MouseEvent} e event
 */
var evt_mMove__ElementID( e ){};

/**
 * @private
 * @function
 * @param {MouseEvent} e event
 */
var evt_mOver__ElementID( e ){};

/**
 * @private
 * @function
 * @param {MouseEvent} e event
 */
var evt_mOut__ElementID( e ){};

/**
 * @private
 * @function
 * @param {MouseEvent} e event
 */
var evt_mUp__ElementID( e ){};